## Developer Notes

### Setup
- `npm install`

### Local Dev
- `npm start` (runs the dev server)

### Build
- `npm run build` (runs `npm run validate:flashcards` first, then builds)

### Flashcard Data
- Flashcards live in `public/*.json`.
- Validate locally with `npm run validate:flashcards`.
